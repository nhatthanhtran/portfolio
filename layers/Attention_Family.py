import torch
import torch.nn as nn

class FullAttention(nn.Module):
    def __init__(self): # it needs more inputs
        super(FullAttention, self).__init__()
        #do more stuffs

    def forward(self): # it needs more inputs
        
        #do stuffs
        pass